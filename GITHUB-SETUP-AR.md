# رفع موقع غسيلك على GitHub

هذه النسخة تحافظ على ملفات الموقع والتصميم وقاعدة البيانات كما وردت في الملف الأصلي.

## الطريقة المناسبة على Windows

1. فك ضغط الملف أولًا إلى مجلد على جهازك.
2. المجلد الرئيسي للمشروع هو المجلد الذي يحتوي على `package.json` و`src` و`public` و`supabase`.
3. أنشئ مستودعًا فارغًا في حسابك على GitHub باسم `ghasilak-website`. يمكنك اختياره Private. لا تضف README أو gitignore أو رخصة أثناء الإنشاء لأن ملفات المشروع جاهزة.
4. افتح Terminal داخل المجلد الرئيسي للمشروع. إذا لم يتوفر أمر `git`، ثبّت Git أولًا.
5. نفّذ الأوامر التالية، مع استبدال `YOUR_USERNAME` باسم حسابك:

```bash
git init
git add .
git commit -m "Prepare GHASILAK website for GitHub"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/ghasilak-website.git
git push -u origin main
```

سجّل الدخول إلى حساب GitHub إذا طلب منك Git ذلك. لا تكتب كلمة مرور أو رمز وصول في ملفات المشروع.

بعد الرفع، تأكد أن `package.json` ظاهر في الصفحة الرئيسية للمستودع، وأن مجلدات `src` و`public` و`supabase` موجودة.

**لا ترفع ملف ZIP وحده إلى المستودع.** ارفع الملفات بعد فك الضغط. استخدام Git ينقل جميع الملفات والمجلدات، بما فيها ملفات الإعدادات التي تبدأ بنقطة، ويطبّق `.gitignore` تلقائيًا.

## تشغيل الموقع على جهازك

استخدم Node.js متوافقًا مع Next.js الموجود في المشروع، ونسخة pnpm المحددة في `package.json`: `10.33.3`.

```bash
npm install -g pnpm@10.33.3
pnpm install --frozen-lockfile
```

انسخ `.env.example` إلى ملف باسم `.env.local`، وأدخل بيانات مشروع Supabase الخاص بك. في PowerShell:

```powershell
Copy-Item .env.example .env.local
```

ثم:

```bash
pnpm dev
```

افتح <http://localhost:43123/ar>.

بدون ربط Supabase لن تعمل وظائف الحسابات والطلبات والبيانات الحية بالكامل. تعليمات قاعدة البيانات والحساب الإداري موجودة في `README.md`.

## نشر الموقع ليعمل عبر رابط

رفع المشروع إلى GitHub يحفظ الكود؛ لا يشغّل الموقع تلقائيًا. المشروع يستخدم Next.js مع وظائف خادم وSupabase، ولذلك يحتاج استضافة تدعم Next.js مثل Vercel. لا تحوّله إلى موقع HTML ثابت لتشغيله على GitHub Pages، لأن وظائف الخادم تعتمد على بيئة تشغيل.

- اربط مستودع GitHub بمشروع Vercel، واجعل المجلد الذي يحتوي على `package.json` هو جذر المشروع.
- استخدم إعداد Next.js وأمر البناء `pnpm build`.
- أضف قيم متغيرات `.env.example` في إعدادات الاستضافة، بما فيها بيانات Supabase.
- اجعل `NEXT_PUBLIC_APP_URL` رابط الموقع الفعلي، واضبط روابط إعادة التوجيه في Supabase وفقًا له.
- طبّق تعليمات قاعدة البيانات الموجودة في `README.md` على مشروع Supabase المقصود. لا تستخدم `db:reset` على قاعدة بيانات فيها بيانات تريد الاحتفاظ بها.

## الملفات الحساسة

ارفع `.env.example` فقط، وهو يحتوي على خانات إعدادات فارغة. ملف `.gitignore` يستبعد `.env.local` وملفات البيئة الفعلية و`node_modules` ومخرجات البناء. يبقى مفتاح `SUPABASE_SERVICE_ROLE_KEY` في إعدادات الخادم، ولا يوضع داخل الكود أو GitHub.

## التعديلات في هذه النسخة

- إضافة هذا الدليل وربطه من `README.md`.
- إضافة `.gitattributes` لضبط التعامل مع الملفات النصية والصور بين أنظمة التشغيل.
- إضافة استثناءات ملفات التخزين المؤقت وملفات النظام إلى `.gitignore`.
- الحفاظ على كود الموقع والصور وملفات Supabase وإصدارات المكتبات وملف القفل دون تغيير.
